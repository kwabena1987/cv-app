name: Docker Workflow

on:
  - push

env:
    CONTAINER_REGISTRY: docker.io
    IMAGE_NAME: cv-app

permissions:
    id-token: write
    contents: write
    packages: write

jobs: 
  docker_build: 
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: GHCR Login
      uses: docker/login-action@v2.2.0
      with:
        registry: ghcr.io
        username: ${{ github.actor }}  
        password: ${{ secrets.GITHUB_TOKEN }}  

    - name: Publish code to GHCR
      run: |
        docker build -t ghcr.io/kwbna-ababi/cv-app:latest .
        docker push ghcr.io/kwbna-ababi/cv-app:latest
